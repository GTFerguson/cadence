# Claude Code Guidelines for ${PROJECT_NAME}

## Git Commit Rules

- Use conventional commit format (feat, fix, refactor, docs, test, chore)
- Do NOT leave marker comments in code or commits (e.g., "Phase 1", "FIX XX:", etc.)
- Keep commit messages clear and descriptive
- Ask for approval before committing — show the proposed message and wait for confirmation

## Project Structure

```
${PROJECT_NAME}/
├── CLAUDE.md                   ← this file (workspace rules)
├── docs/
│   ├── architecture/           ← how the system works + design decisions
│   ├── reference/              ← knowledge base (domain, techniques, foundations)
│   ├── guides/                 ← how to do things (setup, workflows)
│   │   └── tools/             ← tool index (points to co-located docs)
│   └── plans/                  ← forward-looking ideas and roadmaps
├── lib/                         ← shared local modules
│   └── docs/                  ← tool documentation (co-located with source)
├── tools/                       ← standalone utilities
│   ├── doc-index              ← documentation index CLI
│   └── doc_index/             ← index package (parser, search, graph)
└── tests/
```

<!-- Customize this tree to match your actual project layout -->

## Workspace Rules

### Shared vs Owned Areas

| Area | Ownership | Rules |
|------|-----------|-------|
| `docs/architecture/` | Collaborative | Any agent can contribute |
| `docs/reference/` | Collaborative | Any agent can contribute |
| `docs/guides/` | Collaborative | Any agent can contribute |
| `docs/plans/` | Collaborative | Any agent can contribute |
| `lib/docs/` | Collaborative | Tool docs co-located with source |
| `tools/` | Open | Agents can freely add standalone utilities |
| `lib/` | Shared modules | Coordinate changes — don't edit unilaterally |

<!-- Add project-specific owned areas (experiments/, pipelines/, etc.) -->

### Module Lifecycle

Shared code follows a maturity lifecycle:

1. **Prototype** in a workstream directory — quick and dirty, owned by one agent
2. **Stabilise** in `lib/` — extract when useful to multiple workstreams, add tests
3. **Graduate** to a shared package — when the module is mature and needed across repos

### Scope Tags

Use YAML frontmatter so agents can discover relevant docs:

```yaml
---
title: Feature Design Doc
scope: [feature-name]               # single workstream (or [all] for shared)
status: published                   # draft, published, completed
tags: [api, auth]                   # categorisation keywords
description: "Short summary for agent discovery"  # optional, auto-extracted if missing
links: [related-doc, other-guide]   # optional, explicit doc references
created: 2026-02-07
---
```

Agents should use the doc index to find relevant docs: `tools/doc-index --discover`

## Documentation Rules

- **One topic per file** — keep docs focused and scannable
- **Clear filenames** — descriptive kebab-case, no ambiguous abbreviations
- **Frontmatter required** — every doc needs `title`, `scope`, `updated` (or `created`)
- **Reasonable size** — if a doc exceeds ~200 lines, split into focused sub-docs
- **Architecture docs** include decision rationale inline (no separate decisions/ directory)

### Doc Directory Types

| Directory | Purpose |
|-----------|---------|
| `architecture/` | How the system works and why (decisions included) |
| `reference/` | Knowledge base: domain, techniques, foundations |
| `guides/` | How to do things (setup, running tools, workflows) |
| `guides/tools/` | Index of all tools with links to co-located docs |
| `plans/` | Forward-looking ideas and roadmaps |

### Tool Documentation Convention

Tool docs live with the code, not in `docs/guides/`:

- Hub tools (`lib/web_*.py`) → `lib/docs/{tool-name}.md`
- Pipelines → `pipelines/{name}/docs/README.md`
- Standalone scripts (`tools/`) → `docs/guides/{guide-name}.md` (workflow-oriented)
- `docs/guides/tools/README.md` is the master index pointing to all of the above

## Doc Index

Search and query the documentation index:

```bash
# Hybrid fusion search (recommended) — combines fuzzy + TF-IDF + embeddings
python -m tools.doc_index --query "authentication flow" --json

# With graph expansion — also surfaces linked/related docs
python -m tools.doc_index --query "authentication flow" --expand --json

# Discovery: see what scopes, tags, and statuses are available
python -m tools.doc_index --discover --json

# Filter by scope, tag, or status
python -m tools.doc_index --scope feature-name --tag keyword
python -m tools.doc_index --status draft --json

# Build/rebuild the index (run after adding/changing docs)
python -m tools.doc_index --build

# Find related docs via graph traversal
python -m tools.doc_index --related docs/architecture/auth.md --depth 2
```

Use `--query` for all search tasks — it automatically fuses all available signals so you don't need to choose between fuzzy and semantic search.
